# Extra dependencies for this ad-hoc topic only — deliberately NOT merged into
# the app's root requirements.txt (nothing the Streamlit app serves needs them).
# duckdb, pandas, folium and branca are already pinned at the repo root.
numpy>=2.0
